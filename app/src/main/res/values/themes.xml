<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Assessment" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="bottomNavigationStyle">@style/BottomNavigation</item>
        <!-- Customize your light theme here. ->
        <!- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Assessment" parent="Base.Theme.Assessment" />

    <style name="Toolbar.TextAppearance" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:textSize">20sp</item>
        <item name="fontFamily">@font/inter_sb</item>
    </style>

    <style name="BottomNavigation" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="colorPrimary">@color/black</item>
        <item name="fontFamily">@font/inter_reg</item>
        <item name="android:textSize">10sp</item>
        <item name="itemIconTint">@drawable/item_icon_tint</item>
        <item name="itemTextColor">@drawable/item_text_color</item>
        <item name="labelVisibilityMode">labeled</item>
        <item name="android:background">@color/white_f8</item>
    </style>

</resources>